import { Entrant } from '@/src/lib/tournaments';
import { Typography } from '@mui/material';

export default function User() {
  return (
    <Typography
      variant="h5"
      className="text-center"
    >{`Hello World`}</Typography>
  );
}
